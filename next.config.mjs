/** @type {import('next').NextConfig} */
const nextConfig = {
  // Media uploads go through a Route Handler reading the raw request body, so no
  // server-action body-size config is needed here.
};

export default nextConfig;
